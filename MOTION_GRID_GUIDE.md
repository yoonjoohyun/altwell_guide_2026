# ALTWELL SMART GUIDE — 모션그래픽 배치 그리드 가이드

> **목적:** 영상 제작 시 `#motion-canvas`(4:3 모션 박스) 안에 에셋·멤버·카드를 **시나리오만으로 빠르게 배치**하기 위한 공통 좌표계  
> **대상:** 시나리오 작성자 · 씬 구현자 · AI 보조 제작  
> **관련:** `PROJECT_RULE.md` 부록 A · `includes/player/video_layout.asp` · `#motion-canvas`

---

## 1. 모션 박스 개요

| 항목 | 내용 |
|------|------|
| DOM | `#motion-canvas` (`#lo-canvas` → `.lo-stage-frame` 내부) |
| 비율 | **4:3** (`--lo-stage-ratio: 4/3`) |
| 좌표 원점 | 박스 **왼쪽 위** (CSS `left` / `top` 기준) |
| 배치 기준점 | 에셋 **중심** (`transform: translate(-50%, -50%)` 가정) |

씬에서 변경 가능한 영역은 **`#motion-canvas` 내부만**이다.  
프레임·패널·컨트롤러 구조는 변경하지 않는다.

---

## 2. 안전 여백 (Safe Area)

모션 박스 **네 면 모두** 아래 여백을 두고, 그 **안쪽**을 작업 영역으로 사용한다.

| 환경 | 조건 | 상·하·좌·우 여백 |
|------|------|------------------|
| **웹** | `@media (min-width: 901px)` | **10px** |
| **모바일** | `@media (max-width: 900px)` | **5px** |

```
┌─────────────────────────────── #motion-canvas (4:3)
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  ← 웹 10px / 모바일 5px (상)
│ ░┌─────────────────────────┐░
│ ░│                         │░  ← 좌·우 여백
│ ░│    작업 영역 (7×7)       │░
│ ░│                         │░
│ ░└─────────────────────────┘░
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  ← 하단 여백
└───────────────────────────────
```

- 에셋·텍스트·연결선 끝점은 **작업 영역 밖으로 나가지 않게** 배치한다.
- 큰 멤버 유닛은 구역 **중심**보다 안쪽(예: `d4` → 살짝 `c4`)을 쓰는 것도 허용한다.

---

## 3. 7×7 존(Zone) 좌표계

작업 영역을 **가로 7 · 세로 7 = 49칸**으로 나눈다.

### 3-1. 존 이름 규칙

```text
{행문자}{열번호}
```

| 축 | 기호 | 범위 | 방향 |
|----|------|------|------|
| **행 (세로)** | `a` `b` `c` `d` `e` `f` `g` | 7단 | 위 → 아래 |
| **열 (가로)** | `1` `2` `3` `4` `5` `6` `7` | 7단 | 왼쪽 → 오른쪽 |

**예**

| 존 | 위치 |
|----|------|
| `a1` | 왼쪽 위 모서리 |
| `a7` | 오른쪽 위 모서리 |
| `g1` | 왼쪽 아래 모서리 |
| `g7` | 오른쪽 아래 모서리 |
| `d4` | **화면 중심** (작업 영역 정중앙) |
| `a4` | 상단 중앙 |
| `g4` | 하단 중앙 |
| `d1` | 좌측 중앙 |
| `d7` | 우측 중앙 |

### 3-2. 전체 그리드 맵

```text
        1      2      3      4      5      6      7
      ┌──────┬──────┬──────┬──────┬──────┬──────┬──────┐
  a   │  a1  │  a2  │  a3  │  a4  │  a5  │  a6  │  a7  │  ← 상단
      ├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
  b   │  b1  │  b2  │  b3  │  b4  │  b5  │  b6  │  b7  │
      ├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
  c   │  c1  │  c2  │  c3  │  c4  │  c5  │  c6  │  c7  │
      ├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
  d   │  d1  │  d2  │  d3  │  d4  │  d5  │  d6  │  d7  │  ← 중앙 행
      ├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
  e   │  e1  │  e2  │  e3  │  e4  │  e5  │  e6  │  e7  │
      ├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
  f   │  f1  │  f2  │  f3  │  f4  │  f5  │  f6  │  f7  │
      ├──────┼──────┼──────┼──────┼──────┼──────┼──────┤
  g   │  g1  │  g2  │  g3  │  g4  │  g5  │  g6  │  g7  │  ← 하단
      └──────┴──────┴──────┴──────┴──────┴──────┴──────┘
        ↑                                              ↑
      좌측                                            우측
```

### 3-3. 영역 감 잡기 (3×3 요약)

| | 왼쪽 | 중앙 | 오른쪽 |
|---|------|------|--------|
| **위** | a1–a3 | a4–a5 | a6–a7 |
| **중간** | c1–d2 | **d3–e5** | d6–e7 |
| **아래** | f1–g3 | g4–g5 | g6–g7 |

---

## 4. 존 → 좌표 변환

### 4-1. 행·열 숫자 변환

```text
행 a=1  b=2  c=3  d=4  e=5  f=6  g=7
열 1~7 그대로
```

존 `e5` → 행 `5`, 열 `5`

### 4-2. 작업 영역 기준 % (여백 제외)

각 존 **중심점**은 작업 영역 안에서 아래 비율을 따른다.

```text
가로 % = (열 − 0.5) ÷ 7 × 100
세로 % = (행 − 0.5) ÷ 7 × 100
```

| 열 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|----|---|---|---|---|---|---|---|
| **가로 %** | 7.1 | 21.4 | 35.7 | **50.0** | 64.3 | 78.6 | 92.9 |

| 행 | a | b | c | d | e | f | g |
|----|---|---|---|---|---|---|---|
| **세로 %** | 7.1 | 21.4 | 35.7 | **50.0** | 64.3 | 78.6 | 92.9 |

> 소수 첫째 자리 반올림. `d4` = 가로 50% · 세로 50% (작업 영역 중심).

### 4-3. CSS 배치 (여백 반영 · 권장)

```css
/* 웹 — 여백 10px */
#motion-canvas .my-asset {
  position: absolute;
  left: calc(10px + (var(--zone-col) - 0.5) / 7 * (100% - 20px));
  top:  calc(10px + (var(--zone-row) - 0.5) / 7 * (100% - 20px));
  transform: translate(-50%, -50%);
}

/* 모바일 — 여백 5px */
@media (max-width: 900px) {
  #motion-canvas .my-asset {
    left: calc(5px + (var(--zone-col) - 0.5) / 7 * (100% - 10px));
    top:  calc(5px + (var(--zone-row) - 0.5) / 7 * (100% - 10px));
  }
}
```

**예: `b3` (행 2, 열 3)**

```css
--zone-row: 2;
--zone-col: 3;
/* → 작업 영역 기준 약 (35.7%, 21.4%) */
```

### 4-4. 빠른 참조 — 주요 존 중심 좌표 (작업 영역 %)

| 존 | 가로 % | 세로 % | 용도 예 |
|----|--------|--------|---------|
| `a4` | 50.0 | 7.1 | 상단 중앙 타이틀·배지 |
| `b2` | 21.4 | 21.4 | 좌상 2차 포인트 |
| `b6` | 78.6 | 21.4 | 우상 2차 포인트 |
| `d2` | 21.4 | 50.0 | 좌측 중앙 |
| `d4` | 50.0 | 50.0 | **주인공·핵심 오브젝트** |
| `d6` | 78.6 | 50.0 | 우측 중앙 |
| `e3` | 35.7 | 64.3 | 좌하단 보조 |
| `e5` | 64.3 | 64.3 | 우하단 보조 |
| `g4` | 50.0 | 92.9 | 하단 중앙 캡션·타임라인 |

전체 49존 좌표표는 [부록 A — 49존 좌표표](#부록-a--49존-좌표표) 참고.

---

## 5. 시나리오 작성 규칙

`PROJECT_RULE.md` **부록 A** motion 블록에 **존 번호**를 함께 적는다.

### 5-1. 권장 표기

```text
--- motion ---
@0.0s  mount standardStage

@0.3s  show asset.base_business #scene01-base
       @zone d4

@0.6s  show asset.calendar_month #m1
       @zone b2

@0.6s  show asset.point_token #pt1
       @zone b6

@0.9s  animate asset.enter #scene01-base
@0.9s  animate asset.enter #m1
@0.9s  animate asset.enter #pt1
```

| 필드 | 설명 |
|------|------|
| `@zone {행}{열}` | 해당 DOM의 **중심**을 그 존에 맞춤 (예: `@zone d4`) |
| `#id` | 씬 내 고유 ID (기존과 동일) |
| 여러 오브젝트 | 존만 다르게 주면 겹침 없이 배치 가능 |

### 5-2. 배치 패턴 예시

**① 단일 강조 (히어로)**

```text
show member.main #hero @zone d4
```

**② 좌우 대칭**

```text
show asset.autoship      #left  @zone c2
show asset.base_business #right @zone c6
```

**③ 상단 라벨 + 중앙 본체**

```text
show asset.recommend_bonus #badge @zone a4
show member.main             #hero  @zone d4
```

**④ 하단 타임라인 (3칸)**

```text
show asset.calendar_month #m1 @zone f2
show asset.calendar_month #m2 @zone f4
show asset.calendar_month #m3 @zone f6
```

**⑤ 모서리 상태 아이콘**

```text
show asset.status_check #ok @zone a7
show asset.status_cross #no @zone g1
```

### 5-3. 겹침·크기 주의

| 상황 | 권장 |
|------|------|
| 큰 `member` 유닛 | `d4` 또는 `c4`·`e4` — 중심보다 한 칸 위/아래 |
| 가로 긴 배지 | `a4`·`g4` 등 **중앙 열(4)** 사용 |
| 3개 나란히 | 같은 행 + 열 `2` `4` `6` (예: `e2` `e4` `e6`) |
| 2×2 그리드 | `b2` `b6` `e2` `e6` |
| 존 경계 걸침 | **금지** — 다음 칸으로 옮기거나 `--size` 축소 |

---

## 6. 구현자 메모 (씬 JS / CSS)

### 6-1. 존 파싱 헬퍼 (예시)

```javascript
function parseZone(zone) {
  var m = /^([a-g])([1-7])$/i.exec(String(zone || '').trim());
  if (!m) return null;
  return { row: m[1].toLowerCase().charCodeAt(0) - 96, col: parseInt(m[2], 10) };
}

function zoneToPercent(zone) {
  var z = parseZone(zone);
  if (!z) return null;
  return {
    x: ((z.col - 0.5) / 7) * 100,
    y: ((z.row - 0.5) / 7) * 100
  };
}
```

### 6-2. DOM 적용

1. 시나리오 `@zone b3` → `data-zone="b3"` 또는 inline `--zone-row` / `--zone-col`
2. 공통 클래스 `.lo-zone-place` + §4-3 CSS
3. 기존 `left:22%; top:32%` 하드코딩은 **신규 씬에서 존 표기 우선**

### 6-3. 기존 씬과의 대응 (참고)

| 기존 CSS (대략) | 가까운 존 |
|-----------------|-----------|
| `left:50%; top:28%` | `b4` ~ `c4` |
| `left:22%; top:72%` | `f1` ~ `f2` |
| `left:78%; top:72%` | `f6` ~ `f7` |
| `left:50%; top:50%` | `d4` |

---

## 7. 체크리스트

시나리오·씬 완료 전 확인:

- [ ] 모든 에셋에 `@zone` (또는 `#id` + zone 주석)이 있는가
- [ ] 모서리 `a1`·`a7`·`g1`·`g7`에 **큰 객체**를 직접 두지 않았는가
- [ ] 웹 10px / 모바일 5px **안전 여백** 안에 들어오는가
- [ ] 같은 존에 동시에 두 개 이상 배치하지 않았는가
- [ ] 패널(`#lo-panel`) 내용과 모션 좌우 구도가 맞는가

---

## 부록 A — 49존 좌표표

작업 영역 기준 **중심점** (가로 % · 세로 %).  
`d4` = **50.0 · 50.0**

| 존 | 가로 | 세로 | 존 | 가로 | 세로 | 존 | 가로 | 세로 |
|----|------|------|----|------|------|----|------|------|
| a1 | 7.1 | 7.1 | b1 | 7.1 | 21.4 | c1 | 7.1 | 35.7 |
| a2 | 21.4 | 7.1 | b2 | 21.4 | 21.4 | c2 | 21.4 | 35.7 |
| a3 | 35.7 | 7.1 | b3 | 35.7 | 21.4 | c3 | 35.7 | 35.7 |
| a4 | 50.0 | 7.1 | b4 | 50.0 | 21.4 | c4 | 50.0 | 35.7 |
| a5 | 64.3 | 7.1 | b5 | 64.3 | 21.4 | c5 | 64.3 | 35.7 |
| a6 | 78.6 | 7.1 | b6 | 78.6 | 21.4 | c6 | 78.6 | 35.7 |
| a7 | 92.9 | 7.1 | b7 | 92.9 | 21.4 | c7 | 92.9 | 35.7 |
| d1 | 7.1 | 50.0 | e1 | 7.1 | 64.3 | f1 | 7.1 | 78.6 |
| d2 | 21.4 | 50.0 | e2 | 21.4 | 64.3 | f2 | 21.4 | 78.6 |
| d3 | 35.7 | 50.0 | e3 | 35.7 | 64.3 | f3 | 35.7 | 78.6 |
| **d4** | **50.0** | **50.0** | e4 | 50.0 | 64.3 | f4 | 50.0 | 78.6 |
| d5 | 64.3 | 50.0 | e5 | 64.3 | 64.3 | f5 | 64.3 | 78.6 |
| d6 | 78.6 | 50.0 | e6 | 78.6 | 64.3 | f6 | 78.6 | 78.6 |
| d7 | 92.9 | 50.0 | e7 | 92.9 | 64.3 | f7 | 92.9 | 78.6 |
| g1 | 7.1 | 92.9 | — | — | — | — | — | — |
| g2 | 21.4 | 92.9 | | | | | | |
| g3 | 35.7 | 92.9 | | | | | | |
| g4 | 50.0 | 92.9 | | | | | | |
| g5 | 64.3 | 92.9 | | | | | | |
| g6 | 78.6 | 92.9 | | | | | | |
| g7 | 92.9 | 92.9 | | | | | | |

---

## 부록 B — 관련 문서

| 문서 | 내용 |
|------|------|
| `PROJECT_RULE.md` | 시나리오 양식(부록 A) · motion 문법 |
| `ASSET_DESIGN_SYSTEM.md` | 에셋 HTML/CSS 목록 |
| `ARCHITECTURE.md` | 플레이어·씬 런타임 구조 |

---

*문서 버전: #motion-canvas 4:3 · 7×7 존 · 웹 10px / 모바일 5px 여백 (2026-03)*
